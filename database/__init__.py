"""
StudentTrack Pro — Database Package

Submodules:
  connection   — DB_DIR, DB_PATH, get_db()
  schema       — SCHEMA, init_db()
  crud         — insert, update, delete, fetch_all, fetch_one, profiles, settings
  gamification — XP, levels, badges
  db           — Backward-compatible re-export shim
"""
